# JWT Secret Key (required in production)
# Generate a strong random string for production
# Example: openssl rand -hex 32
JWT_SECRET_KEY=

# Frontend Origin (optional, for CORS)
# In production: your deployed frontend URL (e.g., https://your-app.netlify.app)
# In development: leave empty (CORS allows all origins)
FRONTEND_ORIGIN=

# Database URL (optional, defaults to SQLite)
# Local Postgres example (Docker Compose):
# DATABASE_URL=postgresql+psycopg://pingpong:pingpong@localhost:5432/pingpong
DATABASE_URL=

# Flask Environment (optional)
# Set to "production" for production deployment
FLASK_ENV=development
